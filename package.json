{
  "name": "go-clean",
  "private": true,
  "version": "1.0.0",
  "description": "Your storage is full of messy pokémon, go clean!",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev-cli": "cd cli && npm run dev",
    "build": "tsc && vite build",
    "build-cli": "cd cli && npm run build",
    "preview": "vite preview",
    "postinstall": "cd cli && npm install",
    "go-clean": "node ./cli/dist/index.js file --out src/searches.json"
  },
  "dependencies": {
    "solid-js": "^1.8.5"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^4.5.0",
    "vite-plugin-solid": "^2.7.2"
  }
}
